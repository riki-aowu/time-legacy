**时光之遗**

**TIME LEGACY**

Claude 对话档案查看、统计分析与多格式导出工具

产品需求与开发规格 V0.1

2026-08-24

| **一句话定义** 把 Claude 官方导出的聊天记录重新变成一个可阅读、可检索、可统计、可回忆、可导出的私人对话档案馆。 |
|-----------------------------------------------------------------------------------------------------------------|

# 0. 文档用途

本文件可直接交给 Codex、Claude Code、Cursor、Kimi、GLM 等开发型 AI
作为实现规格。目标不是写一个演示页面，而是产出可以实际导入 Claude 官方
JSON/ZIP、浏览历史对话、进行统计分析并完成多格式导出的可用工具。

| **开发原则** 先完成真实数据链路，再做视觉花活。任何“按钮能点但没有真实逻辑”的占位实现，都不算完成。 |
|-----------------------------------------------------------------------------------------------------|

# 1. 产品定位

产品名：时光之遗（Time Legacy）。核心定位是“本地优先的 Claude
Conversation Archive”。用户将 Claude 官方导出的 conversations.json
或导出 ZIP
拖入页面后，系统在浏览器本地完成解压、解析、标准化、索引、统计与展示。

- 不是普通 JSON Viewer：用户不需要面对原始字段和日志结构。

- 不是 Claude
  网页的像素级复制：保留真实对话感，但建立独立的“时光之遗”视觉身份。

- 不是云端聊天同步工具：V0.1 不登录 Claude、不抓取账户、不需要 Claude
  Token。

- 不是服务端数据仓库：默认所有聊天记录只留在用户当前浏览器/本机。

# 2. 产品目标与非目标

## 2.1 必须实现

- 导入 Claude 官方 JSON 与 ZIP 导出包。

- 解析全部会话并建立稳定的内部标准化数据模型。

- 像真正聊天窗口一样浏览每个对话，保留
  Markdown、代码块、表格、引用、附件提示等内容层级。

- 提供全局统计、单窗口统计、时间统计、活跃度、消息长度、排行榜等分析。

- 全文搜索，可从搜索结果直接跳到对应对话的对应消息。

- 支持单窗口与批量多格式导出。

- 大文件可用：几万条消息时不能因为一次性渲染 DOM 而卡死。

- Local First：默认不上传聊天内容到任何服务器。

## 2.2 V0.1 不做

- 不登录或模拟 Claude 账户认证。

- 不做实时与 Claude 官方网站双向同步。

- 不承诺恢复 Claude 导出包里根本不存在的原始图片/PDF 二进制文件。

- 不把“估算 Token”伪装成官方准确 Usage。

- 不根据时间强行猜测缺失的历史模型名称。

# 3. 推荐技术路线

| **层级**   | **推荐方案**                | **目的**                           |
|------------|-----------------------------|------------------------------------|
| 前端       | React + TypeScript + Vite   | 静态 SPA，易于交给多种 AI 持续维护 |
| 数据验证   | Zod                         | 兼容不同 Claude 导出版本并容错     |
| 本地数据库 | IndexedDB + Dexie           | 持久保存已导入档案、索引和统计缓存 |
| ZIP 解析   | zip.js 或 fflate            | 浏览器本地解压，不上传服务器       |
| Markdown   | react-markdown + remark-gfm | 还原正文、表格、列表、引用等       |
| 代码高亮   | Shiki 或 highlight.js       | 代码块可读性与语言标识             |
| 虚拟列表   | @tanstack/react-virtual     | 超长对话避免大 DOM 卡顿            |
| 全文搜索   | MiniSearch（Web Worker 内） | 标题与消息正文索引                 |
| 图表       | ECharts                     | 时间序列、热力图、排行、分布       |
| 日期处理   | date-fns                    | 时间聚合与跨时区显示               |
| 导出       | 原生 Blob/JSZip + Print CSS | MD/TXT/HTML/JSON/ZIP/PDF 等        |

# 4. 总体架构

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<thead>
<tr class="header">
<th>Claude Export ZIP / conversations.json<br />
│<br />
▼<br />
Import &amp; Detection<br />
│<br />
▼<br />
Parser Layer<br />
（版本差异/字段容错）<br />
│<br />
▼<br />
Normalized Data Model<br />
│<br />
┌───────┼────────┐<br />
▼ ▼ ▼<br />
IndexedDB Search Stats Cache<br />
│ Index │<br />
└───────┼───────────┘<br />
▼<br />
Viewer / Analytics / Export</th>
</tr>
</thead>
<tbody>
</tbody>
</table>

关键设计：Claude 原始 JSON 永远不直接耦合 UI。先通过 Parser 转为内部统一
Schema，再由浏览、统计、搜索、导出模块消费。这样 Claude
未来修改导出字段时，只需要维护 Parser 和迁移逻辑。

# 5. 核心用户流程

1\. 用户第一次进入“时光之遗”，看到导入页。

2\. 拖入 Claude 官方 ZIP 或 conversations.json。

3\. 程序识别文件类型、显示大小、会话数量预估，并开始本地解析。

4\. 解析阶段显示真实进度：解压 → 读取 → 标准化 → 建索引 → 统计。

5\. 进入“档案”页：左侧会话列表，右侧当前会话内容。

6\. 用户可搜索、切换窗口、查看窗口统计、收藏/标记（可选 V0.2）。

7\. 进入“统计”查看全局趋势、年度/月度热力与排行榜。

8\. 进入“导出”选择范围和格式，生成本地文件。

9\. 下次打开页面时，可从 IndexedDB 恢复上次导入数据，无需每次重导。

# 6. 信息架构与页面

| **主页面** | **主要功能**                                    | **V0.1** |
|------------|-------------------------------------------------|----------|
| 导入       | 拖拽 ZIP/JSON、导入进度、错误报告、清空本地数据 | 必须     |
| 档案       | 会话列表、聊天浏览、窗口内搜索、消息定位        | 必须     |
| 统计       | 总览、时间、活跃度、消息量、长度、排行、热力图  | 必须     |
| 发现       | 最长窗口、年度关键词、回忆卡片、主题聚类        | V0.2     |
| 导出       | 单/多窗口，MD/TXT/HTML/JSON/PDF/PNG/ZIP         | 必须     |
| 设置       | 主题、时间显示、隐私状态、数据清理、AI 分析配置 | 基础版   |

# 7. 导入与解析规格

## 7.1 支持输入

- Claude 官方导出 ZIP（优先自动寻找 conversations.json）。

- 直接拖入 conversations.json。

- 允许同一用户之后再次导入新版导出包，并提供“覆盖 / 合并”策略。

## 7.2 解析容错

- 字段缺失不能导致全库导入失败；尽量对单条记录降级处理并记录 warning。

- content 可能是字符串、对象数组或不同类型 block；Parser 必须逐类识别。

- 时间字段必须统一为 ISO 时间并保留原始时间值，以避免迁移后失真。

- 未知 content block 不能直接丢弃；保留 raw payload，UI
  可显示“未识别内容”。

- uuid/名称缺失时创建内部 fallback id，但保留 sourceId 字段。

# 8. 内部标准化数据模型

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<thead>
<tr class="header">
<th>type Conversation = {<br />
id: string<br />
sourceId?: string<br />
title: string<br />
createdAt?: string<br />
updatedAt?: string<br />
messages: Message[]<br />
metadata?: Record&lt;string, unknown&gt;<br />
}<br />
<br />
type Message = {<br />
id: string<br />
sourceId?: string<br />
parentId?: string<br />
role: 'user' | 'assistant' | 'system' | 'unknown'<br />
createdAt?: string<br />
blocks: ContentBlock[]<br />
attachments?: AttachmentRef[]<br />
raw?: unknown<br />
}<br />
<br />
type ContentBlock =<br />
| { type: 'text'; text: string }<br />
| { type: 'thinking'; text: string }<br />
| { type: 'code'; code: string; language?: string }<br />
| { type: 'tool_use'; name?: string; payload: unknown }<br />
| { type: 'tool_result'; payload: unknown }<br />
| { type: 'artifact'; artifactId?: string; payload: unknown }<br />
| { type: 'unknown'; payload: unknown }</th>
</tr>
</thead>
<tbody>
</tbody>
</table>

| **注意** 具体 Claude 导出字段以用户实际样本为准。实现时先写 fixture + parser test，不要把上面的示意字段当作唯一真实格式。 |
|---------------------------------------------------------------------------------------------------------------------------|

# 9. “真实对话感”浏览器

档案页是本产品的核心。目标是“重新打开过去那个窗口”的感觉，而不是把 JSON
格式化后展示。

- 左侧：全部会话，按最近更新时间排序；支持搜索、日期筛选、消息数排序。

- 右侧顶部：会话标题、创建时间、更新时间、消息数、窗口分析按钮。

- 正文：Human / Assistant 分层明确，不要求气泡化；更接近高质量 AI
  聊天阅读器。

- 完整渲染
  Markdown：标题、粗体、列表、引用、表格、链接、行内代码、代码块。

- thinking/tool_use/tool_result 默认折叠，用户可展开。

- 附件无法恢复原文件时显示“附件档案卡”：文件名、类型、可获取的提取文本/元数据。

- 代码块支持复制、语言标签；超长代码默认折叠高度并可展开。

- 每条消息有永久定位锚点，搜索结果可直接 scrollToMessage(messageId)。

- 切换会话后保存每个窗口的滚动位置；回来时继续从上次位置阅读。

- 超长会话必须使用虚拟化，不得一次性渲染所有消息节点。

# 10. 分支与重生成处理

如果导出数据存在 parent_message_uuid / parentId
等父子关系，则构建消息树。V0.1
可先选择“默认主线”展示，同时在发生分叉的位置显示分支标记；V0.2
再支持完整分支切换。

- 不要把同一分支中的重生成回复简单当成连续对话，避免统计 turns
  被重复放大。

- 统计模块必须明确“原始消息数”和“主线消息数”的口径。

# 11. 全局统计分析

| **类别** | **指标**                                                                     |
|----------|------------------------------------------------------------------------------|
| 总览     | 总会话数、总消息数、用户消息、Claude 消息、总字符数、活跃天数、首聊/末聊日期 |
| 时间     | 按年/月/周/日的消息量；星期分布；小时分布；GitHub 风格年度热力图             |
| 活跃     | 最长连续活跃天数、最活跃日期、最活跃月份、最常聊天时段                       |
| 长度     | 平均窗口轮数、平均用户消息长度、平均 Claude 回复长度、最长单条消息           |
| 排行     | 消息最多窗口、字符最多窗口、持续跨度最长窗口、代码块最多窗口                 |
| 内容     | 代码块数量、附件数量、thinking/tool block 数量、常见标题词                   |

## 11.1 统计口径

- 所有指标都需要可追溯：点击某个排行榜项目应能跳到对应窗口。

- 字符统计与消息统计使用本地标准化数据，不以 DOM 文本为来源。

- Token 若计算，只能标记“估算”，并注明采用的 tokenizer/估算法；没有官方
  usage 字段时不得显示为精确值。

- 模型字段缺失时显示“未知模型”，不得根据日期强猜。

# 12. 单窗口分析

- 创建时间 / 最后更新时间 / 持续跨度。

- 主线消息数、原始消息数、用户/Claude 消息占比。

- 用户字符数、Claude 字符数、平均消息长度。

- 代码块、附件、thinking、tool、artifact 数量。

- 该窗口的聊天时间分布曲线。

- 窗口内高频词（基础统计即可，V0.1 不要求 AI）。

# 13. 全文搜索

- 搜索范围：会话标题 + 所有文本型消息内容 + 代码文本 +
  可提取的附件文本。

- 索引在 Web Worker 中构建，避免主线程冻结。

- 结果展示：会话标题、角色、日期、命中片段；关键词高亮。

- 点击结果后打开会话并精准定位到 messageId。

- 基础过滤：时间范围、仅用户、仅 Claude、仅代码。

- V0.2 可加入 AND / OR / 精确短语 / 正则或高级查询语法。

# 14. 多格式导出

| **格式** | **用途**             | **实现要求**                             |
|----------|----------------------|------------------------------------------|
| Markdown | 长期归档/喂给其他 AI | 保留角色、时间、标题、代码围栏           |
| TXT      | 最纯净文本           | UTF-8，角色分隔清晰                      |
| HTML     | 离线浏览             | 单文件或资源打包，含基础样式             |
| JSON     | 二次开发             | 支持“标准化 JSON”和“原始 JSON”两种       |
| PDF      | 阅读/打印            | 优先 Print CSS；分页不得切断标题与关键块 |
| PNG      | 长截图/分享          | 超长窗口需分片或给出尺寸限制提示         |
| ZIP      | 批量导出             | 按窗口/月份/年份组织目录                 |

- 允许导出当前窗口、勾选窗口、搜索结果集合、指定时间范围。

- 批量导出文件名需做非法字符清洗与重复名称去重。

- 导出应展示进度，不能在大型档案上无响应。

# 15. Artifacts 与特殊内容

如果实际 Claude 导出中存在 Artifact 创建/修改历史，Parser
应保留这些事件。V0.1 首要任务是“不丢内容”；V0.2 再实现 artifact
replay：按时间顺序应用修改，重建最终文件。

- Artifact 列表：名称/类型/所在会话/最后修改时间。

- 文本型 Artifact：源码查看 + 导出。

- HTML/SVG 等：在严格 sandbox iframe 中预览，禁止继承主站权限。

- 未知/不可执行内容只做源码或下载，不直接运行。

# 16. Local First 与隐私安全

| **硬性要求** 聊天导入、解压、解析、索引、统计默认全部发生在浏览器本地。后端不得收到用户聊天正文。 |
|---------------------------------------------------------------------------------------------------|

- 产品可部署为纯静态站：服务器只提供 HTML/CSS/JS 静态资源。

- 导入页明确显示“本地处理 / 不上传”的状态。

- IndexedDB 中的数据提供“一键彻底清空”。

- 任何可选 AI 深度分析必须默认关闭；开启时清楚说明会发送哪些内容到哪个
  API。

- API Key 仅允许保存在本地，并提供“不保存，仅本次会话使用”选项。

- HTML/Artifact 预览必须防 XSS；不得使用未经消毒的
  dangerouslySetInnerHTML 渲染未知内容。

- 导出的 HTML 若包含原始链接或内容，也要做安全转义。

# 17. 性能要求

| **场景**      | **目标**                                  |
|---------------|-------------------------------------------|
| 10 MB JSON    | 普通桌面浏览器可顺畅导入并迅速进入档案    |
| 100 MB 级导出 | 解析过程中 UI 不冻结；有进度反馈          |
| 1 万+ 消息    | 切换会话与搜索仍可交互                    |
| 超长单窗口    | 使用虚拟列表，不生成海量 DOM              |
| 再次打开      | 从 IndexedDB 恢复，避免强制重新解析原始包 |

- JSON 解析、全文索引、重统计优先放 Web Worker。

- 统计结果与搜索索引要缓存，并以 archiveFingerprint / dataVersion
  控制失效。

- 不要把整个 conversations.json 长期同时保留多份深拷贝在内存中。

# 18. 数据版本与重复导入

- 每次导入计算 archiveFingerprint（可基于文件 hash + size + modified
  time）。

- 内部 schema 有明确 dataVersion，例如 1。

- 再次导入相同文件时提示“已导入”，允许跳过或重建索引。

- 新版导出包与旧库合并时优先 sourceId/uuid 去重；无 id
  时使用稳定组合键并提示可能重复。

- 未来 schema 升级必须写 migration，不允许用户已有档案直接报废。

# 19. UI / 视觉要求

视觉关键词：私人档案馆、时间切片、数字文献、温和克制、长时间阅读舒适。不要做廉价
SaaS 仪表盘，也不要复制 Claude 官方界面到像素级。

- 桌面优先，同时保证 1024px 宽度下可用。

- 主体采用三层：导航 / 会话列表 / 内容阅读区。统计页可切换为大画布布局。

- 聊天阅读区控制最大文字行宽，代码块可横向滚动。

- 统计页优先“数据可读”而非堆图；图表颜色数量要克制。

- 支持深色/浅色模式可放 V0.2；V0.1 至少固定做出一套成熟主题。

- 品牌名必须出现“时光之遗”，英文副标可使用 Time Legacy。

# 20. 页面细节草图（文字版）

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<thead>
<tr class="header">
<th>┌─────────────────────────────────────────────────────────────┐<br />
│ 时光之遗 档案 统计 导出 ⚙ 设置 │<br />
├───────────────┬─────────────────────────────────────────────┤<br />
│ 搜索对话 │ 芥弥架构设计 分析 │<br />
│ │ 2026-08-14 · 386 messages │<br />
│ 最近更新 ├─────────────────────────────────────────────┤<br />
│ ─────────── │ │<br />
│ 芥弥架构设计 │ 你 │<br />
│ 星舟内测问题 │ …… │<br />
│ UI 设计讨论 │ │<br />
│ MBTI 分析 │ Claude │<br />
│ …… │ Markdown / Code / Table / Tool / Artifact │<br />
│ │ │<br />
└───────────────┴─────────────────────────────────────────────┘</th>
</tr>
</thead>
<tbody>
</tbody>
</table>

# 21. 错误处理

| **错误**                  | **用户看到什么**                                | **开发行为**                 |
|---------------------------|-------------------------------------------------|------------------------------|
| 找不到 conversations.json | “未在导出包中找到会话文件”并提示可手动选择 JSON | 不能静默失败                 |
| JSON 格式异常             | 显示文件名、错误阶段、可复制的错误摘要          | 保留日志，不泄露完整私人正文 |
| 部分消息无法解析          | 导入成功，但显示 N 条 warning                   | 记录 sourceId 与 block type  |
| IndexedDB 空间不足        | 提示浏览器存储不足，可切换“临时模式”            | 避免半写入造成库损坏         |
| 导出失败                  | 明确是哪一个窗口/格式失败                       | 其他成功文件不回滚           |

# 22. 可选 AI 深度分析（V0.2+）

AI 分析不是基础统计的前置条件。不开任何 API，也必须完整使用本产品。

- 用户自行配置 Anthropic / OpenAI / Gemini / DeepSeek 等
  API（具体供应商以开发时需求为准）。

- 支持“只分析当前窗口 / 选中窗口 /
  指定时间范围”，禁止默认把整个档案一次性发送。

- 建议通过本地分块 → 摘要 → 聚合的方式分析年度主题、兴趣变化、长期项目。

- AI 生成的画像必须明确标记“AI 分析”，不能与本地客观统计混为一谈。

# 23. “Claude Wrapped / 年度遗迹”方向（V0.2）

- 今年一共开启多少个窗口、聊了多少条消息。

- 最常聊天的月份/星期/小时。

- 年度最长窗口、年度最能“加班”的窗口。

- 年度高频主题/关键词。

- 第一次对话与年末最后一次对话。

- 生成可分享的年度长图，但默认不包含敏感聊天正文。

# 24. V0.1 开发里程碑

| **阶段**    | **范围**                                        | **完成标准**                 |
|-------------|-------------------------------------------------|------------------------------|
| M1 数据链路 | 导入 ZIP/JSON → Parser → 标准化数据 → IndexedDB | 真实样本可导入；刷新后仍存在 |
| M2 档案浏览 | 会话列表 + 聊天渲染 + 虚拟列表 + 消息锚点       | 能舒适阅读大型窗口           |
| M3 搜索     | 本地全文索引 + 命中跳转                         | 搜索结果可精准跳消息         |
| M4 统计     | 总览/时间/热力/排行/单窗口分析                  | 指标口径一致并可点击回溯     |
| M5 导出     | MD/TXT/HTML/JSON/PDF + 批量 ZIP                 | 真实文件能下载并可重新打开   |
| M6 稳定性   | 错误处理、性能、迁移、隐私提示                  | 大样本不崩；清空数据有效     |

# 25. 开发任务拆分（可直接分给多个 AI）

| **Agent**           | **任务**                                                                  |
|---------------------|---------------------------------------------------------------------------|
| Agent A · 数据      | 分析 Claude 样本 JSON；实现 parser、normalizer、Zod schema、fixture、单测 |
| Agent B · 存储/搜索 | Dexie schema、迁移、archive fingerprint、MiniSearch + Worker、消息定位    |
| Agent C · 档案 UI   | 会话列表、聊天渲染器、Markdown/代码/特殊 block、虚拟列表、滚动恢复        |
| Agent D · 统计      | 聚合函数、统计口径、ECharts 页面、热力图、窗口分析                        |
| Agent E · 导出      | Markdown/TXT/HTML/JSON/PDF/ZIP、文件命名与批量进度                        |
| Agent F · QA        | 大型 fixture、异常 JSON、重复导入、XSS、安全、性能回归测试                |

# 26. 推荐代码目录

<table>
<colgroup>
<col style="width: 100%" />
</colgroup>
<thead>
<tr class="header">
<th>src/<br />
├─ app/<br />
├─ features/<br />
│ ├─ import/<br />
│ ├─ archive/<br />
│ ├─ search/<br />
│ ├─ analytics/<br />
│ ├─ export/<br />
│ └─ settings/<br />
├─ data/<br />
│ ├─ parser/<br />
│ ├─ normalizer/<br />
│ ├─ db/<br />
│ ├─ migrations/<br />
│ └─ fixtures/<br />
├─ workers/<br />
│ ├─ import.worker.ts<br />
│ ├─ search.worker.ts<br />
│ └─ stats.worker.ts<br />
├─ components/<br />
│ ├─ message/<br />
│ ├─ charts/<br />
│ └─ common/<br />
├─ lib/<br />
├─ types/<br />
└─ tests/</th>
</tr>
</thead>
<tbody>
</tbody>
</table>

# 27. 最低验收清单

> ☐ 可以拖入真实 Claude conversations.json
> 并看到真实会话，而不是演示数据。
>
> ☐ 可以拖入真实 Claude 导出 ZIP，自动找到会话文件。
>
> ☐ 解析某一条异常消息不会导致整个导入流程失败。
>
> ☐ 左侧会话数量与标准化后的会话数量一致。
>
> ☐ Markdown、代码块、列表、引用、表格能够正确阅读。
>
> ☐ 搜索一个真实聊天词语，可以跳到对应窗口的对应消息。
>
> ☐ 统计总消息数与数据层计算一致，不以 UI 节点数代替。
>
> ☐ 可打开单窗口统计。
>
> ☐ 可导出单窗口 Markdown、TXT、JSON、HTML。
>
> ☐ 可批量导出 ZIP。
>
> ☐ 刷新网页后，本地档案仍能恢复。
>
> ☐ 清空本地数据后 IndexedDB 中不残留聊天正文。
>
> ☐ 导入和统计时 UI 不出现长时间假死。
>
> ☐ 未知 block 被保留或明确标记，没有静默吞掉。
>
> ☐ Token 与模型缺失时不编造数字或模型名。

# 28. 给开发 AI 的执行指令

| **重要** 实现时优先读取真实 Claude 导出样本，再锁定 parser。不要先凭想象写死字段。先让数据走通，再完成 UI。 |
|-------------------------------------------------------------------------------------------------------------|

1\. 初始化 React + TypeScript + Vite 项目与基础路由。

2\. 先制作 /fixtures，并为至少两种可能的消息内容结构写 parser test。

3\. 完成 ZIP/JSON 导入、标准化、IndexedDB
持久化。没有真实数据前不要开始复杂统计图。

4\. 完成档案阅读器与虚拟列表，再做搜索和统计。

5\. 每个按钮必须接真实功能；暂未实现的功能明确标记“Coming
later”，不要伪装可用。

6\. 每完成一个 milestone，使用真实大型导出数据做回归。

7\. 部署可先使用纯静态方式；确认 Network 面板中聊天正文没有被上传。

# 29. 最终交付物要求

- 可运行源码。

- README：安装、开发、构建、部署、本地数据说明。

- 至少一套 parser fixture 与单元测试。

- 生产构建产物。

- 隐私说明：明确本地处理与可选外部 AI 分析的边界。

- CHANGELOG 或版本记录。

# 30. V0.1 成功标准

用户拿到自己的 Claude 官方导出包，不需要理解
JSON，也不需要上传到任何服务，就能在“时光之遗”里重新进入过去的每个对话窗口：看、搜、统计、定位、导出。只要这一条体验真正成立，V0.1
就成功。
