import { openDatabase } from './db'
import type { ImportSession } from './types'

type Row = Record<string, unknown>
export type ArchiveFilter = 'all' | 'conversations' | 'design_chats' | 'attachments' | 'tools' | 'thinking'
export type ArchiveSort = 'updated_desc' | 'created_asc' | 'messages_desc' | 'title_asc'
export type ArchiveIndexItem = { id: string; kind: 'conversation' | 'design_chat'; title: string; createdAt?: string; updatedAt?: string; messageCount: number; hasAttachments: boolean; hasTools: boolean; hasThinking: boolean; projectResolution?: 'resolved' | 'unresolved' | 'absent' }
export type ConversationDetail = { conversation: Row; messages: Array<Row & { blocks: Row[]; attachments: Row[]; files: Row[] }> }
export type SearchResult = { kind: 'conversation' | 'message' | 'project' | 'project_doc' | 'memory_file' | 'project_memory' | 'reflection'; id: string; title: string; excerpt: string; route: string; source: string }
export type MemoryPageData = { globalMemory?: Row; files: Row[]; projectMemories: Row[] }
export type ProjectPageData = { projects: Array<Row & { docsCount: number; hasMemory: boolean }> }
export type DataPageData = { manifests: Row[]; importSessions: ImportSession[]; unknown: Row[]; conflicts: Row[]; sensitiveCounts: { users: number; loginHistory: number } }

function request<T>(value: IDBRequest<T>): Promise<T> { return new Promise((resolve, reject) => { value.onsuccess = () => resolve(value.result); value.onerror = () => reject(value.error) }) }
async function read<T>(names: string[], get: (tx: IDBTransaction) => Promise<T>): Promise<T> { const db = await openDatabase(); try { return await get(db.transaction(names, 'readonly')) } finally { db.close() } }
const rows = <T extends Row = Row>(store: IDBObjectStore) => request(store.getAll() as IDBRequest<T[]>)
const byIndex = <T extends Row = Row>(store: IDBObjectStore, index: string, key: IDBValidKey) => request(store.index(index).getAll(key) as IDBRequest<T[]>)
const text = (value: unknown) => typeof value === 'string' ? value : ''
const blocksFor = (messageId: string, blocks: Row[]) => blocks.filter(block => text(block.messageUuid) === messageId).sort((a, b) => Number(a.blockIndex) - Number(b.blockIndex))

export async function listArchive(options: { filter?: ArchiveFilter; sort?: ArchiveSort; query?: string } = {}): Promise<ArchiveIndexItem[]> {
  const filter = options.filter ?? 'all'; const needle = (options.query ?? '').trim().toLowerCase()
  return read(['conversations', 'messages', 'conversation_blocks', 'attachments', 'design_chats'], async tx => {
    const [conversations, messages, blocks, attachments, designChats] = await Promise.all([rows(tx.objectStore('conversations')), rows(tx.objectStore('messages')), rows(tx.objectStore('conversation_blocks')), rows(tx.objectStore('attachments')), rows(tx.objectStore('design_chats'))])
    const messagesByConversation = new Map<string, Row[]>(); for (const message of messages) messagesByConversation.set(text(message.conversationUuid), [...(messagesByConversation.get(text(message.conversationUuid)) ?? []), message])
    const items: ArchiveIndexItem[] = conversations.map(conversation => { const ownMessages = messagesByConversation.get(text(conversation.uuid)) ?? []; const ids = new Set(ownMessages.map(message => text(message.uuid))); const ownBlocks = blocks.filter(block => ids.has(text(block.messageUuid))); return { id: text(conversation.uuid), kind: 'conversation', title: text(conversation.name) || '未命名对话', createdAt: text(conversation.createdAt) || undefined, updatedAt: text(conversation.updatedAt) || undefined, messageCount: ownMessages.length, hasAttachments: attachments.some(item => ids.has(text(item.messageUuid))), hasTools: ownBlocks.some(block => ['tool_use', 'tool_result'].includes(text(block.type))), hasThinking: ownBlocks.some(block => text(block.type) === 'thinking') } })
    for (const chat of designChats) items.push({ id: text(chat.uuid), kind: 'design_chat', title: text(chat.title) || '未命名 Design Chat', createdAt: text(chat.createdAt) || undefined, updatedAt: text(chat.updatedAt) || undefined, messageCount: Array.isArray(chat.messagesRaw) ? chat.messagesRaw.length : 0, hasAttachments: false, hasTools: false, hasThinking: false, projectResolution: chat.projectResolution as ArchiveIndexItem['projectResolution'] })
    const filtered = items.filter(item => (filter === 'all' || filter === 'conversations' && item.kind === 'conversation' || filter === 'design_chats' && item.kind === 'design_chat' || filter === 'attachments' && item.hasAttachments || filter === 'tools' && item.hasTools || filter === 'thinking' && item.hasThinking) && (!needle || item.title.toLowerCase().includes(needle)))
    const sort = options.sort ?? 'updated_desc'; return filtered.sort((a, b) => sort === 'created_asc' ? (a.createdAt ?? '').localeCompare(b.createdAt ?? '') : sort === 'messages_desc' ? b.messageCount - a.messageCount : sort === 'title_asc' ? a.title.localeCompare(b.title, 'zh-CN') : (b.updatedAt ?? '').localeCompare(a.updatedAt ?? ''))
  })
}

export async function getConversationDetail(conversationUuid: string): Promise<ConversationDetail | undefined> { return read(['conversations', 'messages', 'conversation_blocks', 'attachments', 'files'], async tx => { const conversation = await request(tx.objectStore('conversations').get(conversationUuid) as IDBRequest<Row | undefined>); if (!conversation) return undefined; const messages = await byIndex(tx.objectStore('messages'), 'by_conversation', conversationUuid); const blocks = await rows(tx.objectStore('conversation_blocks')); const attachments = await rows(tx.objectStore('attachments')); const files = await rows(tx.objectStore('files')); return { conversation, messages: messages.sort((a, b) => text(a.createdAt).localeCompare(text(b.createdAt))).map(message => ({ ...message, blocks: blocksFor(text(message.uuid), blocks), attachments: attachments.filter(item => text(item.messageUuid) === text(message.uuid)), files: files.filter(item => text(item.messageUuid) === text(message.uuid)) })) } }) }

export async function getMemories(): Promise<MemoryPageData> { return read(['global_memories', 'memory_files', 'project_memories'], async tx => { const [global, files, projectMemories] = await Promise.all([rows(tx.objectStore('global_memories')), rows(tx.objectStore('memory_files')), rows(tx.objectStore('project_memories'))]); return { globalMemory: global[0], files: files.sort((a, b) => text(a.path).localeCompare(text(b.path))), projectMemories } }) }
export async function listProjects(): Promise<ProjectPageData> { return read(['projects', 'project_docs', 'project_memories'], async tx => { const [projects, docs, memories] = await Promise.all([rows(tx.objectStore('projects')), rows(tx.objectStore('project_docs')), rows(tx.objectStore('project_memories'))]); const output = projects.map(project => ({ ...project, docsCount: docs.filter(doc => text(doc.projectUuid) === text(project.uuid)).length, hasMemory: memories.some(memory => text(memory.projectUuid) === text(project.uuid)) })) as ProjectPageData['projects']; return { projects: output.sort((a, b) => text(b.updatedAt).localeCompare(text(a.updatedAt))) } }) }
export async function getProject(projectUuid: string) { return read(['projects', 'project_docs', 'project_memories'], async tx => ({ project: await request(tx.objectStore('projects').get(projectUuid) as IDBRequest<Row | undefined>), docs: await byIndex(tx.objectStore('project_docs'), 'by_project', projectUuid), memory: (await byIndex(tx.objectStore('project_memories'), 'by_project', projectUuid))[0] })) }
export async function listDesignChats() { return read(['design_chats'], tx => rows(tx.objectStore('design_chats'))) }
export async function listReflections() { return read(['reflections'], async tx => (await rows(tx.objectStore('reflections'))).sort((a, b) => text(b.updatedAt).localeCompare(text(a.updatedAt)))) }
export async function getDataPage(): Promise<DataPageData> { return read(['manifests', 'import_sessions', 'unknown_files', 'conflicts', 'users_metadata', 'login_history'], async tx => { const [manifests, importSessions, unknown, conflicts, users, loginHistory] = await Promise.all([rows(tx.objectStore('manifests')), rows<ImportSession>(tx.objectStore('import_sessions')), rows(tx.objectStore('unknown_files')), rows(tx.objectStore('conflicts')), rows(tx.objectStore('users_metadata')), rows(tx.objectStore('login_history'))]); return { manifests, importSessions: importSessions.sort((a, b) => text(b.startedAt).localeCompare(text(a.startedAt))), unknown, conflicts, sensitiveCounts: { users: users.length, loginHistory: loginHistory.length } } }) }
export async function getSensitiveMetadata() { return read(['users_metadata', 'login_history'], async tx => ({ users: await rows(tx.objectStore('users_metadata')), loginHistory: await rows(tx.objectStore('login_history')) })) }

export async function searchArchive(query: string, options: { includeThinking?: boolean; limit?: number } = {}): Promise<SearchResult[]> { const needle = query.trim().toLowerCase(); if (!needle) return []; const limit = options.limit ?? 100; return read(['conversations', 'messages', 'conversation_blocks', 'projects', 'project_docs', 'memory_files', 'project_memories', 'reflections'], async tx => { const [conversations, messages, blocks, projects, docs, files, memories, reflections] = await Promise.all(['conversations', 'messages', 'conversation_blocks', 'projects', 'project_docs', 'memory_files', 'project_memories', 'reflections'].map(name => rows(tx.objectStore(name)))); const results: SearchResult[] = []; const add = (kind: SearchResult['kind'], id: string, title: string, haystack: string, route: string, source: string) => { const hit = haystack.toLowerCase().indexOf(needle); if (hit >= 0 && results.length < limit) results.push({ kind, id, title, excerpt: haystack.slice(Math.max(0, hit - 80), hit + needle.length + 140), route, source }) }; for (const row of conversations) add('conversation', text(row.uuid), text(row.name), `${text(row.name)}\n${text(row.summary)}`, `/archive/${text(row.uuid)}`, 'Conversation'); for (const row of messages) add('message', text(row.uuid), '对话消息', text(row.text), `/archive/${text(row.conversationUuid)}#${text(row.uuid)}`, 'Conversation'); for (const row of blocks) if (text(row.type) === 'text' || options.includeThinking && text(row.type) === 'thinking') add('message', text(row.messageUuid), text(row.type), text(row.normalizedContent), `/archive/${text(row.conversationUuid)}#${text(row.messageUuid)}`, text(row.type)); for (const row of projects) add('project', text(row.uuid), text(row.name), `${text(row.name)}\n${text(row.description)}\n${text(row.promptTemplate)}`, `/projects/${text(row.uuid)}`, 'Project'); for (const row of docs) add('project_doc', text(row.uuid), text(row.filename), `${text(row.filename)}\n${text(row.content)}`, `/projects/${text(row.projectUuid)}#doc-${text(row.uuid)}`, 'Project Doc'); for (const row of files) add('memory_file', text(row.id), text(row.path), `${text(row.path)}\n${text(row.content)}`, `/memories/files/${text(row.id)}`, 'Memory File'); for (const row of memories) add('project_memory', text(row.id), text(row.projectUuid), text(row.content), `/projects/${text(row.projectUuid)}#memory`, 'Project Memory'); for (const row of reflections) add('reflection', text(row.id), text(row.period), JSON.stringify(row.content ?? ''), `/reflections/${text(row.id)}`, 'Reflection'); return results }) }
